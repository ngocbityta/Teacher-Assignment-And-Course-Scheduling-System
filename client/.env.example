VITE_API_BASE=http://localhost:8080/api

# Example: If your backend runs on a different host/port, update accordingly.
# For Vite dev server (default port 5173) the allowed origin is set in server application.properties
# Do NOT commit real secrets to your own .env files — this example is safe to commit.
VITE_API_BASE=http://localhost:8080/api
